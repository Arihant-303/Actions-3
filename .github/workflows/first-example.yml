name: My first workflow

on: push

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome message
        run: echo "My first github Action job"

      - name: list files
        run: ls

      - name: Read file
        run: cat README.md